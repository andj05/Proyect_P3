@{
    ViewBag.Title = "Login";
    Layout = "~/views/shared/_LayoutApp.cshtml";
}

<style>
    body, html {
        height: 100%;
        margin: 0;
        font-family: 'Segoe UI', 'Roboto', Arial, sans-serif;
        background: url('/Content/Images/Login.jpg') no-repeat center center fixed;
        background-size: cover;
        min-height: 100vh;
    }

    .modern-login-wrapper {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        /* Puedes quitar el background aquí para que no tape la imagen */
        /* background: linear-gradient(120deg, #232526 0%, #414345 100%); */
        position: relative;
    }

        /* Overlay opcional para mejorar la legibilidad */
        .modern-login-wrapper::before {
            content: '';
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: rgba(35, 37, 38, 0.55);
            z-index: 0;
            pointer-events: none;
        }

    .modern-login-card {
        background: #fff;
        border-radius: 28px;
        box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.25);
        padding: 48px 36px 36px 36px;
        max-width: 370px;
        width: 100%;
        display: flex;
        flex-direction: column;
        align-items: center;
        position: relative;
        overflow: hidden;
        transition: box-shadow 0.3s;
        z-index: 1;
    }

        .modern-login-card::before {
            content: '';
            position: absolute;
            top: -60px;
            right: -60px;
            width: 140px;
            height: 140px;
            background: linear-gradient(135deg, #6a82fb 0%, #fc5c7d 100%);
            border-radius: 50%;
            opacity: 0.15;
            z-index: 0;
        }

    .modern-login-header {
        z-index: 1;
        width: 100%;
        text-align: center;
        margin-bottom: 32px;
    }

        .modern-login-header h2 {
            font-size: 2rem;
            font-weight: 700;
            color: #232526;
            margin-bottom: 8px;
            letter-spacing: 1px;
        }

        .modern-login-header span {
            font-size: 2.5rem;
            display: block;
            margin-bottom: 8px;
        }

    .modern-form-group {
        width: 100%;
        margin-bottom: 22px;
        z-index: 1;
    }

        .modern-form-group label {
            font-size: 1rem;
            color: #555;
            margin-bottom: 6px;
            display: block;
            font-weight: 500;
        }

    .modern-form-control {
        width: 100%;
        padding: 13px 16px;
        border: 1.5px solid #e0e0e0;
        border-radius: 12px;
        font-size: 1.05rem;
        background: #f7f8fa;
        transition: border-color 0.2s;
        outline: none;
    }

        .modern-form-control:focus {
            border-color: #6a82fb;
            background: #fff;
        }

    .modern-password-group {
        position: relative;
        width: 100%;
    }

    .modern-password-toggle {
        position: absolute;
        right: 14px;
        top: 50%;
        transform: translateY(-50%);
        background: none;
        border: none;
        color: #888;
        font-size: 1.2rem;
        cursor: pointer;
        z-index: 2;
        padding: 0 4px;
        transition: color 0.2s;
    }

        .modern-password-toggle:hover {
            color: #6a82fb;
        }

    .modern-btn-login {
        width: 100%;
        padding: 14px 0;
        border: none;
        border-radius: 12px;
        background: linear-gradient(90deg, #6a82fb 0%, #fc5c7d 100%);
        color: #fff;
        font-size: 1.1rem;
        font-weight: 700;
        letter-spacing: 1px;
        box-shadow: 0 4px 16px rgba(252, 92, 125, 0.08);
        cursor: pointer;
        transition: background 0.2s, transform 0.2s;
        margin-top: 8px;
    }

        .modern-btn-login:hover {
            background: linear-gradient(90deg, #fc5c7d 0%, #6a82fb 100%);
            transform: translateY(-2px) scale(1.02);
        }

    .modern-loading {
        display: none;
        text-align: center;
        margin-top: 18px;
        font-size: 1rem;
        color: #6a82fb;
        z-index: 1;
    }
</style>

<div class="modern-login-wrapper">
    <div class="modern-login-card">
        <div class="modern-login-header">
            <span>🔐</span>
            <h2>Iniciar Sesión</h2>
        </div>
        <form id="frmAcceder" autocomplete="off" style="width:100%;">
            <div class="modern-form-group">
                <label for="inputUsuario">👤 Usuario</label>
                <input type="text"
                       name="Usuario"
                       id="inputUsuario"
                       class="modern-form-control"
                       placeholder="Ingrese su usuario"
                       required />
            </div>
            <div class="modern-form-group">
                <label for="inputPassword">🔒 Contraseña</label>
                <div class="modern-password-group">
                    <input type="password"
                           name="Password"
                           id="inputPassword"
                           class="modern-form-control"
                           placeholder="Ingrese su contraseña"
                           required />
                    <button type="button" class="modern-password-toggle" id="togglePassword" tabindex="-1" aria-label="Mostrar/Ocultar contraseña">
                        👁️
                    </button>
                </div>
            </div>
            <button type="submit" class="modern-btn-login" id="btnEntrar">
                ➡️ Entrar
            </button>
            <div class="modern-loading" id="loadingSpinner">
                ⏳ Verificando credenciales...
            </div>
        </form>
    </div>
</div>

@section scripts {
    <script>
        $(document).ready(function () {
            $("#inputUsuario").focus();

            $("#togglePassword").click(function () {
                const passwordField = $("#inputPassword");
                const isPassword = passwordField.attr("type") === "password";
                passwordField.attr("type", isPassword ? "text" : "password");
                $(this).text(isPassword ? "🙈" : "👁️");
            });

            $("#frmAcceder").submit(function (e) {
                e.preventDefault();

                $("#btnEntrar").prop("disabled", true).text("⏳ Entrando...");
                $("#loadingSpinner").show();

                const Url = "@Url.Content("~/Acceder/Enter")";
                const parametros = $(this).serialize();

                $.post(Url, parametros, function (data) {
                    if (data == "1") {
                        window.location.href = "@Url.Action("Index", "Home")";
                    } else {
                        alert("❌ " + data);
                    }
                })
                .fail(function () {
                    alert("❌ Error de conexión. Intente nuevamente.");
                })
                .always(function () {
                    $("#btnEntrar").prop("disabled", false).text("➡️ Entrar");
                    $("#loadingSpinner").hide();
                });
            });
        });
    </script>
}